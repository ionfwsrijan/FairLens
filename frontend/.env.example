# Server-side proxy target used by frontend/app/api/audit/route.ts.
FAIRLENS_API_URL=http://localhost:8000

# Optional: set this only if you want the browser to call the backend directly.
# NEXT_PUBLIC_API_URL=http://localhost:8000
